import apiClientAdvanced from "./api-client-advanced";

// const mode: 'simple' | 'advanced' = 'simple';

const apiClient = apiClientAdvanced; // Default export for simplicity

export { apiClient }; // Export both for flexibility